# 개발 계획서: P-13 운동 진행 화면 단순화

---

## 📌 개발 목표

- 운동 진행 화면에서 불필요한 컨트롤과 설명성 정보를 제거한다.
- 사용자가 운동 중에는 `세트 완료`와 `휴식 처리`에만 집중하도록 단순화한다.
- 작은 글씨/과다 정보 문제를 해소해 가독성과 터치 정확도를 높인다.

---

## 🔧 변경 원칙

1. 운동 중 화면은 학습 화면이 아니다.
- `운동 부위`, 설명 문구, 보조 안내 문장은 기본 화면에서 제외한다.

2. 핵심 행동은 하나다.
- Primary 액션은 `세트 완료`만 유지한다.
- `일시정지`, `중단` 버튼은 제거한다.

3. 정보는 크게, 적게 보여준다.
- 현재 운동명, 세트 진행(예: `2/4`), 휴식 타이머만 노출한다.
- 숫자(세트/타이머)는 큰 타이포로 표시한다.

---

## 🧩 대상 컴포넌트

- `apps/web-client/src/pages/workout/WorkoutView.tsx`
- `apps/web-client/src/pages/workout/Workout.tsx`
- 관련 테스트 파일
  - `apps/web-client/src/pages/workout/WorkoutView.test.tsx`
  - `apps/web-client/src/pages/workout/Workout.test.tsx`

---

## 📝 작업 항목

### 1) UI 단순화
- [ ] 상단 액션 버튼(`일시정지`, `중단`) 제거
- [ ] 운동 부위/중량·시간 보조 라벨의 기본 노출 최소화
- [ ] 핵심 요소만 남긴 레이아웃으로 재정렬

### 2) 타이포/인터랙션 개선
- [ ] 세트 진행 숫자와 휴식 타이머를 대형 타이포로 변경
- [ ] `세트 완료` 버튼을 모바일 하단 고정(엄지 영역 + safe area)
- [ ] 휴식 중에는 `휴식 건너뛰기`만 명확하게 표시

### 3) 상태/동작 정리
- [ ] 제거된 버튼에 대응되는 핸들러(`onPauseWorkout`, `onResumeWorkout`, `onAbandonWorkout`) 정리
- [ ] 기존 세트 완료/휴식/다음 운동 전환 플로우 유지

### 4) 테스트 업데이트
- [ ] 제거된 버튼 관련 테스트 정리
- [ ] 단순화된 핵심 정보 렌더링 테스트 추가
- [ ] 하단 고정 CTA 및 큰 타이포(핵심 숫자) 검증 추가

---

## ✅ 완료 기준

- [ ] 운동 진행 화면에 `일시정지/중단` 버튼이 없다
- [ ] 핵심 정보(운동명, 세트 진행, 휴식 타이머)만 표시된다
- [ ] 모바일에서 `세트 완료` 버튼이 하단 엄지 영역에 배치된다
- [ ] 관련 테스트 통과 및 기존 플로우 회귀 없음
