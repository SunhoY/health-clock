# 개발 계획서: P-19 근력운동 세트별 수행정보 ViewModel 도입

---

## 📌 개발 목표

- 근력운동 데이터를 세트 단위로 저장/표시할 수 있는 구조로 전환한다.
- 각 세트의 `중량`과 `횟수`를 독립적으로 입력/수정 가능하게 한다.
- 루틴 생성 플로우(S-04 → S-05 → S-06)에서 세트별 정보가 누락 없이 누적되도록 한다.

---

## 🔧 변경 원칙

1. 근력운동은 세트별 데이터가 기본이다.
- 운동 전체에 단일 중량/단일 횟수를 두지 않는다.

2. 세트 수가 바뀌면 데이터 구조도 함께 갱신한다.
- 예: 3세트 → 4세트 변경 시 4번째 세트 초기값 추가

3. 입력 중인 데이터는 다음 화면으로 그대로 전달한다.
- 운동 추가/복귀 시 입력값이 초기화되지 않도록 관리한다.

---

## 🧩 ViewModel 제안

### 근력운동
- [ ] `StrengthExerciseSetViewModel`
  - `setNumber: number`
  - `weight: number | null`
  - `reps: number | null`
- [ ] `StrengthExerciseInputViewModel`
  - `exerciseId: string`
  - `exerciseName: string`
  - `sets: StrengthExerciseSetViewModel[]`

### 유산소운동
- [ ] `CardioExerciseInputViewModel`
  - `exerciseId: string`
  - `exerciseName: string`
  - `durationMinutes: number | null`

### 루틴 생성 누적 모델
- [ ] `RoutineExerciseDraftViewModel`
  - `type: 'strength' | 'cardio'`
  - `strength?: StrengthExerciseInputViewModel`
  - `cardio?: CardioExerciseInputViewModel`

---

## 🧩 대상 컴포넌트

- `apps/web-client/src/pages/exercise-detail/ExerciseDetail.tsx`
- `apps/web-client/src/pages/exercise-detail/ExerciseDetailView.tsx`
- `apps/web-client/src/types/exercise.ts`
- 필요 시 루틴 draft 저장/전달 로직 파일
- 관련 테스트 파일
  - `apps/web-client/src/pages/exercise-detail/ExerciseDetail.test.tsx`
  - `apps/web-client/src/pages/exercise-detail/ExerciseDetailView.test.tsx`

---

## 📝 작업 항목

### 1) 타입/모델 리팩터링
- [ ] 기존 단일 `weight/reps` 입력 구조를 세트 배열 기반으로 변경
- [ ] 세트별 유효성 규칙 정의
  - [ ] 각 세트 `weight`, `reps` 필수 여부
  - [ ] 0/음수 값 제한

### 2) 화면 입력 구조 변경 (S-05)
- [ ] 근력운동 입력 UI를 세트별 행 편집 구조로 변경
- [ ] 세트 수 변경 시 행 추가/삭제 처리
- [ ] 유산소는 기존 단일 시간 입력 유지

### 3) 상태 누적/전달
- [ ] 운동 추가 시 세트별 입력값이 draft에 반영되는지 확인
- [ ] S-04로 돌아갔다가 재진입해도 입력값 복원 가능하도록 상태 유지 전략 수립

### 4) 테스트 보강
- [ ] 근력운동 세트별 입력/수정 시 상태 반영 검증
- [ ] 세트 수 증감 시 데이터 구조 갱신 검증
- [ ] 유산소 입력 흐름 회귀 없음 검증
- [ ] 루틴 저장 전 최종 payload에 세트별 값이 포함되는지 검증

---

## ✅ 완료 기준

- [ ] 근력운동은 세트별 `중량/횟수`를 독립적으로 저장/표시한다
- [ ] 루틴 생성 흐름에서 세트별 정보가 끊기지 않고 누적된다
- [ ] 유산소 입력 흐름은 기존 기능을 유지한다
- [ ] 관련 테스트가 추가/수정되고 통과한다
