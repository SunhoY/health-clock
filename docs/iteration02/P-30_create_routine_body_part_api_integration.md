# 개발 계획서: P-30 루틴 만들기 부위 목록 API 연동

---

## 📌 개발 목표

- S-03(루틴 만들기) 화면의 운동 부위 목록 소스를 하드코딩 상수에서 서버 API로 전환한다.
- 화면 진입 시 `GET /api/exercises/body-parts` 응답을 기준으로 부위 버튼을 렌더링한다.
- API 실패 시에도 사용자 흐름이 끊기지 않도록 로딩/에러 상태를 명확히 처리한다.

---

## 🔧 변경 원칙

1. 부위 목록은 서버 단일 소스를 사용한다.
- `CreateRoutine`의 `BODY_PARTS` 하드코딩 배열을 제거하고 API 응답으로 대체한다.

2. UI 안정성을 위해 표시 순서는 고정한다.
- DB 조회 결과는 `chest, back, legs, shoulders, arms, abs, calves, fullbody, cardio` 순서로 정렬해 사용자 경험을 유지한다.

3. API 경계에서 표시용 모델로 변환한다.
- 응답 DTO를 바로 뷰에 전달하지 않고 `id`, `name` 중심의 화면 모델로 매핑한다.

4. 기존 편집 진입 플로우를 깨지 않는다.
- `mode=edit` 진입 시 기존처럼 `/exercise-selection/edit` 리다이렉트가 우선 동작해야 한다.

---

## 🧩 대상 문서/코드

- 시나리오
  - `docs/user-scenario/S-03_create_routine.md`
- BE
  - `apps/nest-backend/src/app/app.module.ts`
  - `apps/nest-backend/src/app/exercises/exercises.module.ts` (신규)
  - `apps/nest-backend/src/app/exercises/exercises.controller.ts` (신규)
  - `apps/nest-backend/src/app/exercises/exercises.service.ts` (신규)
  - `apps/nest-backend/src/app/exercises/exercises.repository.ts` (신규)
  - `apps/nest-backend/src/app/exercises/dto/body-part.dto.ts` (신규)
- FE
  - `apps/web-client/src/pages/create-routine/CreateRoutine.tsx`
  - `apps/web-client/src/pages/create-routine/CreateRoutineView.tsx`
  - `apps/web-client/src/pages/create-routine/CreateRoutine.test.tsx`

---

## 📝 작업 항목

### 1) 시나리오 정합성 반영
- [ ] S-03 문서에서 부위 목록이 API 응답 기반임을 명시
- [ ] 로딩/조회 실패 시 사용자 노출 상태(재시도) 문구 정리

### 2) 백엔드 부위 조회 API 구현
- [ ] `GET /api/exercises/body-parts` 엔드포인트 추가
- [ ] `exercises` 테이블에서 `is_active=true` 기준 부위 목록 조회
- [ ] 중복 제거 + 고정 순서 정렬 + 응답 DTO(`id`, `name`) 구성
- [ ] 조회 실패 시 5xx 에러 처리

### 3) 프론트 S-03 API 연동
- [ ] `CreateRoutine`에서 부위 목록 API 호출 로직 추가
- [ ] 로딩 중에는 버튼 영역에 로딩 상태 표시
- [ ] 실패 시 에러 문구 + 재시도 버튼 제공
- [ ] 성공 시 API 응답으로 부위 버튼 렌더링
- [ ] 부위 클릭 시 기존 라우팅(`/exercise-selection/:bodyPart`) 유지

### 4) 테스트 보강
- [ ] BE: exercises controller/service 단위 테스트 추가
- [ ] FE: API 성공/실패/재시도 흐름 테스트 추가
- [ ] FE: edit 모드 리다이렉트가 API 로직보다 우선 동작하는지 검증

---

## ✅ 완료 기준

- [ ] S-03 부위 목록이 하드코딩 없이 API 응답으로 렌더링된다
- [ ] API 실패 시 사용자에게 실패 상태와 재시도 수단이 제공된다
- [ ] edit 진입 리다이렉트 플로우가 유지된다
- [ ] 관련 테스트가 통과한다

---

## 📎 API 응답 예시

```json
[
  { "id": "chest", "name": "가슴" },
  { "id": "back", "name": "등" },
  { "id": "legs", "name": "하체" },
  { "id": "shoulders", "name": "어깨" },
  { "id": "arms", "name": "팔" },
  { "id": "abs", "name": "코어(복부)" },
  { "id": "calves", "name": "종아리" },
  { "id": "fullbody", "name": "전신" },
  { "id": "cardio", "name": "유산소" }
]
```
