# 개발 계획서: P-20 세트 입력 UX 개선 및 미입력 자동 채움

---

## 📌 개발 목표

- 근력운동 세트 입력 UI를 더 작고 빠르게 조작 가능한 형태로 개선한다.
- `횟수(reps)` 표기를 `횟수`로 단순화한다.
- 한 세트에 입력한 값이 아직 입력되지 않은 다른 세트에 자동 반영되도록 해 반복 입력 피로를 줄인다.

---

## 🔧 변경 원칙

1. 자동 채움은 `undefined` 상태에만 적용한다.
- 사용자가 한 번이라도 입력한 세트 값은 자동 채움 대상에서 제외한다.

2. 자동 채움은 항목 단위로 동작한다.
- 중량 변경은 중량 필드에만, 횟수 변경은 횟수 필드에만 반영한다.

3. 직접 입력과 증감 버튼은 동일한 상태 업데이트 규칙을 사용한다.
- 경로가 달라도 결과 상태와 검증 규칙은 동일해야 한다.

---

## 🧩 대상 문서/코드

- 시나리오
  - `docs/user-scenario/S-05_create_routine_exercise_detail.md`
- 페이지
  - `apps/web-client/src/pages/exercise-detail/ExerciseDetail.tsx`
  - `apps/web-client/src/pages/exercise-detail/ExerciseDetailView.tsx`
- 타입(필요 시)
  - `apps/web-client/src/types/exercise.ts`
- 테스트
  - `apps/web-client/src/pages/exercise-detail/ExerciseDetail.test.tsx`
  - `apps/web-client/src/pages/exercise-detail/ExerciseDetailView.test.tsx`

---

## 📝 작업 항목

### 1) 라벨/표현 정리
- [ ] `횟수(reps)` 라벨을 `횟수`로 변경
- [ ] 접근성 이름(`aria-label`)도 동일하게 정리

### 2) 입력 UI 밀도 개선
- [ ] 중량/횟수 입력 칸 높이와 폭을 축소해 한 화면 정보 밀도 개선
- [ ] 각 입력 항목에 좌측 `-`, 우측 `+` 버튼 배치
- [ ] 증감 단위 적용
  - [ ] 중량: 5kg 단위
  - [ ] 횟수: 1 단위
- [ ] 숫자 직접 입력(Key in) 유지

### 3) 자동 채움 로직
- [ ] 상태 모델에 "해당 필드가 입력된 적 있는지"를 판별 가능한 구조 유지/추가
- [ ] 특정 세트 필드 변경 시 동일 필드의 `undefined` 세트에 값 자동 반영
- [ ] 자동 채움 제외 조건 적용
  - [ ] 이미 입력 이력이 있는 세트
  - [ ] 현재 직접 변경 중인 세트(원본 값 유지)
- [ ] `+/-` 변경 시에도 동일 자동 채움 규칙 적용

### 4) 검증/회귀 테스트
- [ ] `횟수` 라벨 변경 회귀 테스트
- [ ] 중량 `+/-5`, 횟수 `+/-1` 동작 테스트
- [ ] 직접 입력 동작 테스트
- [ ] 자동 채움 테스트
  - [ ] 1세트 입력 시 2/3세트의 `undefined` 값 자동 채움
  - [ ] 이미 입력 이력이 있는 값은 유지됨
- [ ] 라우팅/저장 payload 회귀 테스트

---

## ✅ 완료 기준

- [ ] `횟수(reps)` 표기가 화면에서 제거되고 `횟수`로 통일된다
- [ ] 중량/횟수 입력은 `+/-`와 직접 입력이 모두 가능하다
- [ ] 한 세트에 입력한 값이 미입력 세트에만 자동 채워진다
- [ ] 이미 입력한 값은 자동 채움으로 덮어써지지 않는다
- [ ] 관련 테스트가 추가/수정되고 통과한다
