# 개발 계획서: P-15 운동 간 전환(운동완료/다음 운동/스톱워치) 도입

---

## 📌 개발 목표

- 한 운동의 마지막 세트 완료 후, 바로 다음 운동으로 넘어가기 전에 짧은 전환 단계를 제공한다.
- 전환 단계에서 사용자가 필요한 최소 정보만 확인할 수 있도록 `운동완료`, `다음 운동 정보`, `휴식 스톱워치`를 표시한다.
- 별도 입력 없이 `[다음 운동 시작]` 버튼 하나로 다음 운동으로 진입하도록 단순화한다.

---

## 🔧 변경 원칙

1. 입력을 요구하지 않는다.
- 운동 간 전환 화면은 설정/입력 폼 없이 정보 표시 + 단일 CTA만 제공한다.

2. 지금 끝난 운동과 다음 운동을 명확히 분리한다.
- 상단은 완료된 운동(과거), 하단은 다음 운동(미래) 정보로 구분한다.

3. 시간 정보는 카운트다운이 아닌 경과형으로 제공한다.
- 다음 운동 시작 전까지의 휴식 시간을 스톱워치(00:00 → 증가)로 표시한다.

---

## 🧩 대상 컴포넌트

- `apps/web-client/src/pages/workout/Workout.tsx`
- `apps/web-client/src/pages/workout/WorkoutView.tsx`
- 필요 시 타입 정의 파일 (`apps/web-client/src/types/exercise.ts`)
- 관련 테스트 파일
  - `apps/web-client/src/pages/workout/Workout.test.tsx`
  - `apps/web-client/src/pages/workout/WorkoutView.test.tsx`

---

## 📝 작업 항목

### 1) 상태 모델 확장
- [ ] 운동 상태에 `exerciseTransition` 단계 추가
  - 예: `intraSetRest`(세트 간 휴식) / `betweenExercises`(운동 간 전환)
- [ ] 전환 단계에 필요한 ViewModel 필드 정의
  - 완료 운동명(예: `푸시업 완료`)
  - 다음 운동명 + 지표(`weight/reps/duration`)
  - 스톱워치 경과 시간
  - CTA 라벨(`다음 운동 시작`)

### 2) Workout 로직 변경
- [ ] 마지막 세트 완료 시 분기 처리
  - 다음 운동이 있으면 즉시 다음 운동으로 인덱스를 넘기지 않고 `betweenExercises` 단계 진입
  - 다음 운동이 없으면 기존처럼 완료 플로우(S-08) 이동
- [ ] `betweenExercises` 진입 시 스톱워치 시작
- [ ] `[다음 운동 시작]` 클릭 시
  - 다음 운동 인덱스로 이동
  - 세트를 1로 초기화
  - 스톱워치 종료 및 상태 초기화

### 3) WorkoutView UI 확장
- [ ] `betweenExercises` 전용 블록 추가
  - `운동완료` 메시지
  - 다음 운동 정보
  - 스톱워치
  - `[다음 운동 시작]` 버튼
- [ ] 다음 운동 지표는 `undefined` 항목 미노출 규칙 유지
- [ ] 기존 세트 간 휴식 UI와 시각적으로 구분되도록 계층 유지

### 4) 타이머/클린업 안정화
- [ ] 기존 카운트다운 타이머와 스톱워치 타이머 동시 실행 방지
- [ ] 화면 전환/언마운트 시 interval 정리
- [ ] 버튼 연타 시 중복 전환 방지

### 5) 테스트 보강
- [ ] `Workout.test.tsx`
  - [ ] 마지막 세트 완료 후 전환 화면이 표시되는지 검증
  - [ ] 전환 화면에서 다음 운동 정보가 표시되는지 검증
  - [ ] 스톱워치가 증가하는지 검증(fake timers)
  - [ ] `[다음 운동 시작]` 클릭 시 다음 운동 1세트로 진입하는지 검증
  - [ ] 마지막 운동 완료 시에는 전환 화면 없이 S-08로 이동하는지 검증
- [ ] `WorkoutView.test.tsx`
  - [ ] 전환 화면 UI 요소(완료 메시지/다음 운동/스톱워치/버튼) 렌더링 검증
  - [ ] 다음 운동 지표 `undefined` 미노출 검증

---

## ✅ 완료 기준

- [ ] 한 운동 종료 후 다음 운동이 있을 때 전환 화면이 노출된다
- [ ] 전환 화면에 `운동완료`, 다음 운동 정보, 스톱워치, `[다음 운동 시작]` 버튼이 표시된다
- [ ] `[다음 운동 시작]`으로만 다음 운동 진행이 가능하다
- [ ] 다음 운동 지표는 `undefined`일 때 표시되지 않는다
- [ ] 관련 테스트가 추가/수정되고 통과한다
