# 화면기획서: S-05 루틴 만들기 - 세트별 수행 정보 입력 화면

---

## 📌 개요

* **화면명**: 루틴 만들기 - 세트별 수행 정보 입력
* **목적**: 선택한 운동의 세트별 중량/횟수 정보를 입력해 실제 수행 계획을 구성한다

---

## 📂 구성 요소

* **제목**: `[선택한 운동명]`
* **근력 운동 입력 항목**:

  * 세트 수 선택 (기본 3세트)
  * 세트별 입력 행(1세트, 2세트, 3세트 ...)
    * 중량(kg)
    * 횟수
  * 각 입력 항목(중량/횟수) 좌우 증감 버튼 제공
    * 중량: `-5kg / +5kg`
    * 횟수: `-1 / +1`
  * 숫자 직접 입력(Key in)도 함께 지원

* **유산소 운동 입력 항목**:

  * 시간(분)

> 근력 운동은 세트마다 `중량/횟수`가 다를 수 있으므로 세트별로 독립 입력한다.

* **하단 고정 버튼**:

  * `[완료]` (Primary): S-06 제목 입력 팝업
  * `[운동 더 추가]` (Secondary): S-04로 이동

* **모바일 UI 기준**:

  * 세트별 입력 행은 스크롤해도 세트 번호가 쉽게 구분되도록 구성
  * 과도하게 큰 입력창 대신 콤팩트한 컨트롤로 구성
  * 숫자 직접 입력(숫자 키패드)과 증감 버튼을 함께 제공
  * 필수값 누락 시 해당 세트 행에 즉시 짧은 에러 문구 노출
  * 한 화면에 과한 부가정보 없이 `운동명 + 세트별 입력`에 집중

---

## 🔁 동작

* 입력 완료 후 `[완료]` 클릭 시 ➝ S-06 제목 입력 팝업
* `[운동 더 추가]` 클릭 시 ➝ S-04 세부 운동 선택 화면
* 저장되는 운동 정보는 세트별 `중량/횟수`를 포함한 구조로 누적된다
* 초기 상태에서 각 세트의 중량/횟수는 `undefined`로 취급된다
* 특정 세트에 값이 입력되면, 아직 한 번도 입력되지 않은(= `undefined`) 다른 세트의 같은 항목은 해당 값으로 자동 채워진다
* 이미 사용자가 입력한 이력이 있는 세트 값은 자동 채움으로 덮어쓰지 않는다
