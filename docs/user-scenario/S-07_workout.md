# 화면기획서: S-07 운동 진행 화면

---

## 📌 개요

* **화면명**: 운동 진행
* **목적**: 현재 세트 수행과 휴식 흐름만 빠르게 처리한다

---

## 📂 구성 요소

* **상단 핵심 정보**:
  * 현재 운동명
  * 현재 세트 진행 상태 (예: 2/4)

* **메인 행동 영역**:

  * `[세트 완료]` 버튼 (Primary)

* **휴식 타이머 영역** (세트 완료 후 자동 표시):

  * 남은 시간 표시
  * 상단: 이전 세트 정보 표시 (예: `이전 1/3 세트 · 20kg · 10회`)
  * 하단: 다음 세트 정보 표시 (예: `다음 2/3 세트 · 20kg · 10회`)
  * 표시 위계
    * 이전 정보: 더 작은 텍스트, 회색 톤
    * 다음 정보: 더 큰 텍스트, 긍정 강조 컬러
  * 각 영역은 필요한 값만 노출 (`무게`, `횟수`, `시간` 중 `undefined`가 아닌 항목만 표시)
  * `[휴식 건너뛰기]` 버튼

* **운동 간 전환 영역** (한 운동의 마지막 세트 완료 시):

  * `운동완료` 상태 메시지 표시 (예: `푸시업 완료`)
  * 다음 운동 정보 표시 (운동명 + 필요 지표)
  * 운동 간 휴식 스톱워치 표시 (다음 운동 시작까지 경과 시간)
  * `[다음 운동 시작]` 버튼

* **모바일 UI 기준**:

  * 핵심 버튼은 하단 엄지 영역에 고정
  * 버튼/타이머/세트 숫자는 크게 표시
  * 운동 중 학습성 정보(운동 부위 설명, 보조 안내 문구)는 노출하지 않음
  * 한 화면 핵심 행동은 `세트 완료` 하나로 유지

---

## 🔁 동작

* `[세트 완료]` 클릭 시 ➝ 휴식 타이머 활성화
  * 휴식 화면 상단에 `이전 n/m 세트`와 직전 수행값(`무게/횟수/시간`)을 표시
  * 휴식 화면 하단에 `다음 n/m 세트`와 다음 수행값(`무게/횟수/시간`)을 강조 표시
* 휴식 종료 후 ➝ 다음 세트 진행
* 마지막 세트 완료 시
  * 다음 운동이 있으면 ➝ 운동 간 전환 영역 표시 (`운동완료`, 다음 운동 정보, 스톱워치, `[다음 운동 시작]`)
  * `[다음 운동 시작]` 클릭 시 ➝ 다음 운동의 `1세트` 화면으로 진입
* 마지막 운동까지 완료 시 ➝ S-08 운동 완료 화면 이동
