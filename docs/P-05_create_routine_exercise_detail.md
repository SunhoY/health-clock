# 개발 계획서: P-05 루틴 만들기 - 세부 항목 입력 화면

---

## 📌 개발 목표

- 선택된 운동의 세트 수, 중량, 시간을 입력할 수 있는 직관적인 인터페이스 구현
- 스피너 방식의 숫자 입력 컨트롤을 통한 빠른 데이터 입력 지원
- 웨이트 운동과 유산소 운동에 따른 조건부 입력 필드 처리
- 루틴에 운동을 추가하거나 루틴 생성을 완료하는 플로우 제어

---

## 🏗️ 컴포넌트 구조

### Presentational Components
- `ExerciseDetailView`: 운동 상세 입력 화면 전체 레이아웃
- `ExerciseTitle`: 선택된 운동명을 표시하는 제목 컴포넌트
- `NumberSpinner`: 숫자 증감을 위한 스피너 컴포넌트
- `ExerciseForm`: 운동 상세 정보 입력 폼
- `ActionButtons`: 하단 액션 버튼들 (끝내기, 운동 추가하기)

### Container Components
- `ExerciseDetailContainer`: 입력 데이터 관리 및 폼 제출 로직 담당

---

## ⚙️ 필요한 기능 및 상태 관리

### 상태 관리
- 선택된 운동 정보 (이전 단계에서 전달)
- 입력된 운동 상세 정보 (세트 수, 중량, 시간)
- 폼 유효성 검사 상태
- 제출 상태 (로딩, 성공, 에러)

### 주요 기능
- 운동 타입에 따른 조건부 필드 표시 (유산소는 중량 입력 비활성화)
- 스피너를 통한 숫자 입력 (세트: 1씩, 중량: 5씩, 시간: 1씩 증감)
- 폼 유효성 검사 (최소값, 최대값 검증)
- 운동 추가 및 루틴 완료 로직
- 임시 저장 및 복원 기능

### 데이터 모델
```typescript
interface ExerciseDetail {
  exerciseId: string;
  exerciseName: string;
  bodyPart: string;
  sets: number;
  weight?: number; // 유산소 운동일 경우 null
  duration?: number; // 유산소 운동일 경우 필수
  restTime?: number; // 세트 간 휴식 시간 (기본값 설정)
}

interface FormState {
  sets: number;
  weight: number;
  duration: number;
  isValid: boolean;
  errors: {
    sets?: string;
    weight?: string;
    duration?: string;
  };
}

interface FormConfig {
  sets: {
    min: number;
    max: number;
    step: number;
    default: number;
  };
  weight: {
    min: number;
    max: number;
    step: number;
    default: number;
  };
  duration: {
    min: number;
    max: number;
    step: number;
    default: number;
  };
}
```

---

## 🧪 테스트 계획

### Unit Tests (`ExerciseDetailView.test.tsx`)
- [ ] 운동명이 제목에 올바르게 표시
- [ ] 웨이트 운동일 때 모든 필드가 표시
- [ ] 유산소 운동일 때 중량 필드가 비활성화
- [ ] 액션 버튼들이 올바르게 렌더링

### Unit Tests (`NumberSpinner.test.tsx`)
- [ ] 초기값이 올바르게 표시
- [ ] 증가 버튼 클릭 시 설정된 step만큼 증가
- [ ] 감소 버튼 클릭 시 설정된 step만큼 감소
- [ ] 최소값/최대값 범위 내에서만 변경
- [ ] 직접 입력 시 유효성 검사
- [ ] 키보드 네비게이션 지원

### Unit Tests (`ExerciseForm.test.tsx`)
- [ ] 폼 데이터가 올바르게 바인딩
- [ ] 유효성 검사 에러 메시지 표시
- [ ] 폼 제출 시 콜백 함수 호출
- [ ] 필드 간 의존성 처리

### Integration Tests (`ExerciseDetailContainer.test.tsx`)
- [ ] 폼 데이터 변경 시 상태 업데이트
- [ ] 운동 추가 시 루틴 데이터에 올바르게 추가
- [ ] 루틴 완료 시 제목 입력 단계로 이동
- [ ] 에러 상태 처리

---

## 📚 스토리북 계획

### Stories (`ExerciseDetailView.stories.tsx`)
- [ ] WeightExercise: 웨이트 운동 (벤치프레스) 입력 화면
- [ ] CardioExercise: 유산소 운동 (러닝머신) 입력 화면
- [ ] WithErrors: 유효성 검사 에러가 있는 상태
- [ ] Loading: 제출 중 로딩 상태

### Stories (`NumberSpinner.stories.tsx`)
- [ ] Default: 기본 스피너
- [ ] WithCustomStep: 커스텀 증감 단위
- [ ] WithRange: 최소/최대값이 설정된 스피너
- [ ] Disabled: 비활성화된 스피너
- [ ] WithError: 에러 상태의 스피너

### Stories (`ExerciseForm.stories.tsx`)
- [ ] WeightForm: 웨이트 운동 폼
- [ ] CardioForm: 유산소 운동 폼
- [ ] WithValidation: 유효성 검사가 있는 폼

---

## 📝 개발 순서 및 할 일 목록

### 1단계: 핵심 컴포넌트 구현
- [ ] `NumberSpinner` 컴포넌트 구현
- [ ] 스피너 로직 (증감, 범위 검증) 구현
- [ ] 키보드 입력 및 접근성 지원

### 2단계: 폼 컴포넌트 구현
- [ ] `ExerciseForm` 컴포넌트 구현
- [ ] 조건부 필드 렌더링 로직
- [ ] 폼 유효성 검사 로직
- [ ] 에러 메시지 표시 시스템

### 3단계: 테스트 작성
- [ ] `NumberSpinner` 단위 테스트 작성
- [ ] `ExerciseForm` 단위 테스트 작성
- [ ] 유효성 검사 로직 테스트
- [ ] 접근성 테스트

### 4단계: 스토리북 설정
- [ ] `NumberSpinner` 스토리 작성
- [ ] `ExerciseForm` 스토리 작성
- [ ] 다양한 운동 타입별 스토리
- [ ] 인터랙션 테스트 추가

### 5단계: 상태 관리 및 비즈니스 로직
- [ ] 폼 상태 관리 훅 구현
- [ ] 운동 상세 정보 검증 로직
- [ ] 루틴 데이터 업데이트 로직
- [ ] 임시 저장/복원 기능

### 6단계: Container Component 구현
- [ ] `ExerciseDetailContainer` 구현
- [ ] 폼 제출 로직 구현
- [ ] 라우팅 로직 (운동 추가 vs 루틴 완료)
- [ ] 에러 핸들링

### 7단계: 전체 화면 구성
- [ ] `ExerciseDetailView` 전체 레이아웃 구현
- [ ] 액션 버튼 영역 구현
- [ ] 반응형 디자인 적용

### 8단계: 통합 테스트 및 최적화
- [ ] 전체 플로우 테스트
- [ ] 성능 최적화 (메모이제이션)
- [ ] UX 개선 (애니메이션, 피드백)

---

## 🔧 기술적 고려사항

### 의존성
- React Hook Form 또는 Formik (폼 관리)
- Yup 또는 Zod (스키마 검증)
- 루틴 생성 컨텍스트

### 파일 구조
```
src/
  pages/
    create-routine/
      exercise-detail/
        components/
          ExerciseDetailView.tsx
          ExerciseDetailView.test.tsx
          ExerciseDetailView.stories.tsx
          ExerciseForm.tsx
          ExerciseForm.test.tsx
          ExerciseForm.stories.tsx
          NumberSpinner.tsx
          NumberSpinner.test.tsx
          NumberSpinner.stories.tsx
          ActionButtons.tsx
          ExerciseTitle.tsx
        containers/
          ExerciseDetailContainer.tsx
          ExerciseDetailContainer.test.tsx
        hooks/
          useExerciseForm.ts
          useExerciseForm.test.ts
        validation/
          exerciseValidation.ts
          exerciseValidation.test.ts
        index.ts
```

### 폼 설정 및 검증
```typescript
const FORM_CONFIG: FormConfig = {
  sets: { min: 1, max: 10, step: 1, default: 3 },
  weight: { min: 0, max: 500, step: 5, default: 20 },
  duration: { min: 1, max: 180, step: 1, default: 30 }
};

const validationSchema = {
  sets: (value: number) => value >= 1 && value <= 10,
  weight: (value: number, isCardio: boolean) => isCardio || (value >= 0 && value <= 500),
  duration: (value: number, isCardio: boolean) => !isCardio || (value >= 1 && value <= 180)
};
```

### 접근성 고려사항
- 스피너 버튼에 적절한 ARIA 레이블
- 폼 필드와 에러 메시지 연결
- 키보드 네비게이션 지원
- 스크린 리더를 위한 상태 변화 알림

---

## ✅ 완료 기준

- [ ] 모든 단위 테스트 통과
- [ ] 통합 테스트 통과
- [ ] TypeScript 컴파일 에러 없음
- [ ] ESLint 규칙 준수
- [ ] 스토리북에서 모든 상태 확인 가능
- [ ] 웨이트 운동과 유산소 운동에 따른 조건부 필드 처리
- [ ] 스피너를 통한 정확한 증감 동작
- [ ] 폼 유효성 검사 및 에러 처리
- [ ] 운동 추가 및 루틴 완료 플로우 정상 동작
- [ ] 반응형 디자인 구현
- [ ] 접근성 기준 충족 (WCAG 2.1 AA)
- [ ] 키보드만으로 모든 기능 사용 가능
- [ ] 직관적이고 사용하기 쉬운 인터페이스 